# 🏙️ 中国城市探险网站 - 项目架构与设计文档

## 📋 目录
1. [视觉风格板](#视觉风格板)
2. [Next.js 项目结构](#nextjs-项目结构)
3. [页面组件清单](#页面组件清单)
4. [Tailwind CSS 样式规范](#tailwind-css-样式规范)
5. [首页设计结构](#首页设计结构)
6. [动画建议](#动画建议)

---

## 🎨 视觉风格板

### 颜色系统 (Color Palette)

```javascript
// tailwind.config.js 颜色配置
colors: {
  // 主色调 - 暗色系
  dark: {
    900: '#0a0a0a',    // 纯黑背景
    800: '#121212',    // 深灰背景
    700: '#1a1a1a',    // 卡片背景
    600: '#242424',    // 边框/分割线
    500: '#2d2d2d',    // 悬停状态
  },
  
  // 霓虹点缀色 - 低饱和
  neon: {
    cyan: '#00ffff',      // 青色霓虹（主要强调）
    purple: '#9d4edd',    // 紫色霓虹（次要强调）
    orange: '#ff6b35',    // 橙色霓虹（警告/危险）
    green: '#06ffa5',     // 绿色霓虹（安全/通过）
  },
  
  // 灰度系统
  gray: {
    50: '#fafafa',
    100: '#f5f5f5',
    200: '#e5e5e5',
    300: '#d4d4d4',
    400: '#a3a3a3',
    500: '#737373',      // 主要文本
    600: '#525252',      // 次要文本
    700: '#404040',      // 禁用文本
    800: '#262626',
    900: '#171717',
  },
  
  // 功能色
  danger: '#dc2626',     // 危险提示
  warning: '#f59e0b',    // 警告提示
  success: '#10b981',    // 成功状态
}
```

### 字体系统

```javascript
fontFamily: {
  sans: ['Inter', 'system-ui', 'sans-serif'],      // 正文
  display: ['Space Grotesk', 'sans-serif'],        // 标题/强调
  mono: ['JetBrains Mono', 'monospace'],           // 代码/数据
}
```

### 间距系统

```javascript
spacing: {
  // 基于 4px 网格系统
  '0.5': '0.125rem',   // 2px
  '1': '0.25rem',      // 4px
  '2': '0.5rem',       // 8px
  '3': '0.75rem',      // 12px
  '4': '1rem',         // 16px
  '6': '1.5rem',       // 24px
  '8': '2rem',         // 32px
  '12': '3rem',        // 48px
  '16': '4rem',        // 64px
  '24': '6rem',        // 96px
  '32': '8rem',        // 128px
}
```

### 视觉氛围关键词

- **主调**: 暗黑、废墟、工业、废弃
- **质感**: 粗糙、颗粒感、低对比度
- **光效**: 局部霓虹、微弱光晕、阴影深邃
- **排版**: 极简、留白充足、层次分明
- **交互**: 克制、精准、微反馈

---

## 📁 Next.js 项目结构

```
urbex_web/
├── .next/                          # Next.js 构建输出
├── public/
│   ├── images/                     # 静态图片资源
│   │   ├── cities/                 # 城市相关图片
│   │   ├── locations/              # 地点相关图片
│   │   ├── gallery/                # 画廊图片
│   │   └── gear/                   # 装备图片
│   ├── fonts/                      # 自定义字体
│   └── favicon.ico
│
├── src/
│   ├── app/                        # Next.js 13+ App Router
│   │   ├── layout.tsx              # 根布局
│   │   ├── page.tsx                # 首页
│   │   ├── globals.css             # 全局样式
│   │   │
│   │   ├── archive/                # 🏙️ 城市档案库
│   │   │   ├── page.tsx            # 档案库首页（省份列表）
│   │   │   ├── [province]/         # 省份动态路由
│   │   │   │   ├── page.tsx        # 省份页面（城市列表）
│   │   │   │   └── [city]/         # 城市动态路由
│   │   │   │       └── page.tsx    # 单城市详情页
│   │   │   └── loading.tsx         # 加载状态
│   │   │
│   │   ├── locations/              # 🔍 地点索引
│   │   │   ├── page.tsx            # 地点瀑布流首页
│   │   │   ├── [id]/               # 地点详情页
│   │   │   │   └── page.tsx
│   │   │   └── loading.tsx
│   │   │
│   │   ├── gear/                   # 🎒 装备专区
│   │   │   ├── page.tsx            # 装备列表页
│   │   │   ├── [slug]/             # 单装备详情页
│   │   │   │   └── page.tsx
│   │   │   └── loading.tsx
│   │   │
│   │   ├── guidebook/              # 📘 探险指南
│   │   │   ├── page.tsx            # 指南首页
│   │   │   ├── safety/             # 安全章节
│   │   │   │   └── page.tsx
│   │   │   ├── ethics/             # 伦理章节
│   │   │   │   └── page.tsx
│   │   │   └── rules/              # 规则章节
│   │   │       └── page.tsx
│   │   │
│   │   ├── gallery/                # 🖼️ 摄影作品画廊
│   │   │   ├── page.tsx            # 画廊首页
│   │   │   ├── [id]/               # 单图详情/全屏
│   │   │   │   └── page.tsx
│   │   │   └── loading.tsx
│   │   │
│   │   ├── stories/                # 🧭 专题与故事
│   │   │   ├── page.tsx            # 故事列表页
│   │   │   ├── [slug]/             # 单故事详情页
│   │   │   │   └── page.tsx
│   │   │   └── loading.tsx
│   │   │
│   │   ├── about/                  # ⚒️ 关于
│   │   │   └── page.tsx
│   │   │
│   │   └── admin/                  # 📁 素材输入接口（未来）
│   │       ├── page.tsx            # 管理后台入口
│   │       ├── upload/             # 上传功能
│   │       │   └── page.tsx
│   │       └── layout.tsx          # 管理后台布局
│   │
│   ├── components/                 # React 组件
│   │   ├── layout/                 # 布局组件
│   │   │   ├── Header.tsx          # 网站头部导航
│   │   │   ├── Footer.tsx          # 网站底部
│   │   │   ├── Navigation.tsx      # 主导航菜单
│   │   │   └── MobileMenu.tsx      # 移动端菜单
│   │   │
│   │   ├── archive/                # 城市档案库组件
│   │   │   ├── ProvinceCard.tsx    # 省份卡片
│   │   │   ├── CityCard.tsx        # 城市卡片
│   │   │   ├── CityHero.tsx        # 城市页 Hero
│   │   │   ├── LocationGrid.tsx    # 地点网格
│   │   │   ├── LocationCard.tsx    # 地点卡片
│   │   │   ├── CityMap.tsx         # 城市地图（静态）
│   │   │   └── DangerAlert.tsx     # 危险提示组件
│   │   │
│   │   ├── locations/              # 地点索引组件
│   │   │   ├── LocationMasonry.tsx # 瀑布流布局
│   │   │   ├── LocationCard.tsx    # 地点卡片（复用）
│   │   │   ├── DifficultyBadge.tsx # 难度等级徽章
│   │   │   ├── TagList.tsx         # 标签列表
│   │   │   └── FilterBar.tsx       # 筛选栏
│   │   │
│   │   ├── gear/                   # 装备专区组件
│   │   │   ├── GearCard.tsx        # 装备卡片
│   │   │   ├── GearGrid.tsx        # 装备网格
│   │   │   ├── GearDetail.tsx      # 装备详情
│   │   │   ├── ScenarioList.tsx    # 适用场景列表
│   │   │   └── RiskNotice.tsx      # 风险说明
│   │   │
│   │   ├── guidebook/              # 探险指南组件
│   │   │   ├── GuideSection.tsx    # 指南章节
│   │   │   ├── RuleCard.tsx        # 规则卡片
│   │   │   ├── SafetyWarning.tsx   # 安全警告
│   │   │   └── TableOfContents.tsx # 目录导航
│   │   │
│   │   ├── gallery/                # 画廊组件
│   │   │   ├── GalleryGrid.tsx     # 画廊网格
│   │   │   ├── ImageCard.tsx       # 图片卡片
│   │   │   ├── Lightbox.tsx        # 全屏查看器
│   │   │   ├── ImageViewer.tsx     # 图片查看器
│   │   │   └── ImageSlider.tsx     # 图片轮播
│   │   │
│   │   ├── stories/                # 故事组件
│   │   │   ├── StoryCard.tsx       # 故事卡片
│   │   │   ├── StoryHero.tsx       # 故事 Hero
│   │   │   ├── StoryContent.tsx    # 故事内容
│   │   │   └── ImageGallery.tsx    # 故事内图片画廊
│   │   │
│   │   ├── ui/                     # 通用 UI 组件
│   │   │   ├── Button.tsx          # 按钮
│   │   │   ├── Card.tsx            # 卡片容器
│   │   │   ├── Badge.tsx           # 徽章
│   │   │   ├── Tag.tsx             # 标签
│   │   │   ├── Modal.tsx           # 模态框
│   │   │   ├── Loading.tsx         # 加载动画
│   │   │   ├── Skeleton.tsx        # 骨架屏
│   │   │   ├── Divider.tsx         # 分割线
│   │   │   └── Icon.tsx            # 图标组件
│   │   │
│   │   └── home/                   # 首页专用组件
│   │       ├── Hero.tsx            # 首页 Hero
│   │       ├── SectionCard.tsx     # 模块入口卡片
│   │       ├── ModuleGrid.tsx      # 模块网格
│   │       └── FeaturedGallery.tsx # 精选画廊预览
│   │
│   ├── lib/                        # 工具函数
│   │   ├── utils.ts                # 通用工具
│   │   ├── constants.ts            # 常量定义
│   │   ├── types.ts                # TypeScript 类型
│   │   └── data/                   # 数据文件（临时，未来接入 CMS）
│   │       ├── cities.ts           # 城市数据
│   │       ├── locations.ts        # 地点数据
│   │       ├── gear.ts             # 装备数据
│   │       └── stories.ts          # 故事数据
│   │
│   ├── hooks/                      # 自定义 Hooks
│   │   ├── useScroll.ts            # 滚动监听
│   │   ├── useImageLoad.ts         # 图片加载
│   │   ├── useLightbox.ts          # 全屏查看器
│   │   └── useFilter.ts            # 筛选逻辑
│   │
│   └── styles/                     # 样式文件
│       └── animations.css          # 自定义动画
│
├── .env.local                      # 环境变量
├── .gitignore
├── next.config.js                  # Next.js 配置
├── tailwind.config.js              # Tailwind 配置
├── tsconfig.json                   # TypeScript 配置
├── package.json
└── README.md
```

---

## 🧩 页面组件清单

### 1. 🏙️ 城市档案库 (City Archive)

#### 档案库首页 (`/archive`)
- `ProvinceCard` - 省份卡片（显示省份名称、城市数量、缩略图）
- `ProvinceGrid` - 省份网格布局

#### 省份页面 (`/archive/[province]`)
- `CityCard` - 城市卡片（城市名、简介、地点数量、封面图）
- `CityGrid` - 城市网格布局
- `Breadcrumb` - 面包屑导航

#### 单城市页面 (`/archive/[province]/[city]`)
- `CityHero` - 城市 Hero 区域（大图背景、城市名、简介）
- `LocationGrid` - 地点网格（该城市的所有地点）
- `LocationCard` - 地点卡片（图片、名称、难度、标签）
- `CityMap` - 静态地图组件（显示地点分布）
- `DangerAlert` - 危险提示横幅
- `CityStats` - 城市统计（地点数、难度分布等）

---

### 2. 🔍 地点索引 (Locations Index)

#### 地点列表页 (`/locations`)
- `FilterBar` - 筛选栏（按省份、城市、难度、标签筛选）
- `LocationMasonry` - 瀑布流布局容器
- `LocationCard` - 地点卡片（大图、标题、难度徽章、标签、悬停效果）
- `DifficultyBadge` - 难度等级徽章（⚠️ 1-5级）
- `TagList` - 标签列表（废弃工厂、医院、学校等）
- `LoadingSkeleton` - 加载骨架屏

#### 地点详情页 (`/locations/[id]`)
- `LocationHero` - 地点 Hero（大图、标题、位置信息）
- `LocationInfo` - 地点信息卡片（描述、坐标、难度、标签）
- `ImageGallery` - 图片画廊（该地点的所有照片）
- `DangerWarning` - 危险警告组件
- `RelatedLocations` - 相关地点推荐

---

### 3. 🎒 装备专区 (Gear Zone)

#### 装备列表页 (`/gear`)
- `GearGrid` - 装备网格布局
- `GearCard` - 装备卡片（图片、名称、分类、简要说明）
- `CategoryFilter` - 分类筛选（照明、防护、拍摄、其他）

#### 装备详情页 (`/gear/[slug]`)
- `GearHero` - 装备 Hero（大图、名称、分类）
- `GearDetail` - 装备详情（功能说明、适用场景、注意事项）
- `ScenarioList` - 适用场景列表（卡片式展示）
- `RiskNotice` - 风险说明组件
- `RelatedGear` - 相关装备推荐

---

### 4. 📘 探险指南 (Guidebook)

#### 指南首页 (`/guidebook`)
- `GuideSection` - 指南章节卡片（安全、伦理、规则）
- `QuickLinks` - 快速链接导航

#### 指南章节页 (`/guidebook/[section]`)
- `TableOfContents` - 目录导航（侧边栏）
- `GuideContent` - 指南内容（Markdown 渲染）
- `RuleCard` - 规则卡片（重要规则突出显示）
- `SafetyWarning` - 安全警告框（红色边框、图标）
- `CodeBlock` - 代码块样式（用于展示示例）

---

### 5. 🖼️ 摄影作品画廊 (Visual Gallery)

#### 画廊首页 (`/gallery`)
- `GalleryGrid` - 画廊网格（大图展示，悬停显示标题）
- `ImageCard` - 图片卡片（懒加载、悬停效果）
- `CategoryTabs` - 分类标签（按城市、主题分类）

#### 图片详情页 (`/gallery/[id]`)
- `ImageViewer` - 图片查看器（全屏、缩放、导航）
- `ImageInfo` - 图片信息（拍摄地点、时间、设备）
- `ImageNavigation` - 图片导航（上一张/下一张）
- `Lightbox` - 全屏灯箱模式

---

### 6. 🧭 专题与故事 (Stories)

#### 故事列表页 (`/stories`)
- `StoryCard` - 故事卡片（封面图、标题、摘要、标签）
- `StoryGrid` - 故事网格布局
- `ThemeFilter` - 主题筛选

#### 故事详情页 (`/stories/[slug]`)
- `StoryHero` - 故事 Hero（大图背景、标题、作者、日期）
- `StoryContent` - 故事内容（富文本、图片穿插）
- `ImageGallery` - 故事内图片画廊
- `StoryMeta` - 故事元信息（标签、相关地点、阅读时间）
- `RelatedStories` - 相关故事推荐

---

### 7. ⚒️ 关于 (About)

#### 关于页面 (`/about`)
- `AboutHero` - 关于 Hero（网站理念大标题）
- `MissionSection` - 使命说明
- `ValuesSection` - 价值观展示（卡片式）
- `Disclaimer` - 特别声明（强调安全、反对破坏）
- `ContactInfo` - 联系方式（可选）

---

### 8. 📁 素材输入接口 (Admin)

#### 管理后台 (`/admin`)
- `AdminLayout` - 管理后台布局
- `UploadForm` - 上传表单（图片、数据）
- `ContentEditor` - 内容编辑器
- `DataTable` - 数据表格（管理已有内容）

---

## 🎨 Tailwind CSS 样式规范

### 基础配置

```javascript
// tailwind.config.js
module.exports = {
  content: ['./src/**/*.{js,ts,jsx,tsx}'],
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        // 使用上面定义的颜色系统
        dark: { /* ... */ },
        neon: { /* ... */ },
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
        display: ['Space Grotesk', 'sans-serif'],
        mono: ['JetBrains Mono', 'monospace'],
      },
      spacing: {
        // 使用上面定义的间距系统
      },
      borderRadius: {
        'none': '0',
        'sm': '0.125rem',
        'DEFAULT': '0.25rem',
        'md': '0.375rem',
        'lg': '0.5rem',
        'xl': '0.75rem',
        '2xl': '1rem',
      },
      boxShadow: {
        'glow-cyan': '0 0 20px rgba(0, 255, 255, 0.3)',
        'glow-purple': '0 0 20px rgba(157, 78, 221, 0.3)',
        'inner-dark': 'inset 0 2px 4px 0 rgba(0, 0, 0, 0.5)',
      },
      backdropBlur: {
        xs: '2px',
      },
    },
  },
  plugins: [],
}
```

### 常用样式类

```css
/* globals.css 补充样式 */

/* 霓虹文字效果 */
.text-neon-cyan {
  color: #00ffff;
  text-shadow: 0 0 10px rgba(0, 255, 255, 0.5),
               0 0 20px rgba(0, 255, 255, 0.3);
}

.text-neon-purple {
  color: #9d4edd;
  text-shadow: 0 0 10px rgba(157, 78, 221, 0.5),
               0 0 20px rgba(157, 78, 221, 0.3);
}

/* 卡片悬停效果 */
.card-hover {
  transition: all 0.3s ease;
}

.card-hover:hover {
  transform: translateY(-4px);
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
}

/* 图片遮罩渐变 */
.image-overlay {
  background: linear-gradient(
    to bottom,
    transparent 0%,
    rgba(0, 0, 0, 0.7) 100%
  );
}

/* 边框霓虹效果 */
.border-neon {
  border: 1px solid rgba(0, 255, 255, 0.3);
  box-shadow: 0 0 10px rgba(0, 255, 255, 0.2);
}
```

---

## 🏠 首页设计结构

### Hero 区域

```
┌─────────────────────────────────────────┐
│                                         │
│         [大图背景 - 城市废墟]            │
│                                         │
│         [主标题: 中国城市探险]           │
│         [副标题: 探索遗落的空间]         │
│                                         │
│         [向下滚动提示]                  │
│                                         │
└─────────────────────────────────────────┘
```

**组件**: `Hero`
- 全屏高度（100vh）
- 背景图片 + 暗色遮罩
- 居中文字，霓虹色标题
- 平滑滚动动画

---

### 模块入口区域

```
┌─────────────────────────────────────────┐
│  [模块入口网格 - 2x4 布局]               │
│                                         │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐│
│  │ 🏙️   │  │ 🔍   │  │ 🎒   │  │ 📘   ││
│  │ 城市  │  │ 地点  │  │ 装备  │  │ 指南  ││
│  │ 档案  │  │ 索引  │  │ 专区  │  │      ││
│  └──────┘  └──────┘  └──────┘  └──────┘│
│                                         │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐│
│  │ 🖼️   │  │ 🧭   │  │ ⚒️   │  │      ││
│  │ 画廊  │  │ 故事  │  │ 关于  │  │      ││
│  └──────┘  └──────┘  └──────┘  └──────┘│
└─────────────────────────────────────────┘
```

**组件**: `ModuleGrid` + `SectionCard`
- 每个卡片：图标 + 标题 + 描述 + 悬停效果
- 卡片背景：深色 + 边框霓虹效果
- 点击跳转到对应页面

---

### 精选画廊预览

```
┌─────────────────────────────────────────┐
│  [精选作品]                              │
│                                         │
│  [大图网格 - 3-4 张精选图片]             │
│  [每张图片悬停显示标题和地点]            │
│                                         │
│  [查看全部 →]                            │
└─────────────────────────────────────────┘
```

**组件**: `FeaturedGallery`
- 展示 3-4 张精选图片
- 大图展示，悬停显示信息
- 底部链接到完整画廊

---

### 视觉焦点区域

```
┌─────────────────────────────────────────┐
│  [大图展示 - 单张或轮播]                 │
│  [配合文字说明：探索的意义]              │
│                                         │
└─────────────────────────────────────────┘
```

**组件**: `VisualFocus`
- 全宽大图
- 文字叠加（底部渐变遮罩）
- 营造氛围感

---

## ✨ 动画建议

### 微动画原则
- **克制**: 动画时长 200-400ms
- **自然**: 使用 ease-out 缓动
- **精准**: 只对关键交互做动画

### 具体动画

1. **页面进入动画**
   - 淡入：`opacity: 0 → 1` (300ms)
   - 轻微上移：`translateY: 20px → 0` (300ms)

2. **卡片悬停动画**
   - 上移：`translateY: 0 → -4px` (200ms)
   - 阴影加深：`box-shadow` 变化 (200ms)
   - 边框霓虹：`border-color` 变化 (200ms)

3. **图片加载动画**
   - 骨架屏 → 淡入图片 (400ms)
   - 使用 `next/image` 的 `blur` 占位

4. **滚动触发动画**
   - 使用 Intersection Observer
   - 元素进入视口时淡入 + 上移

5. **导航菜单动画**
   - 移动端菜单：侧滑进入 (300ms)
   - 下拉菜单：淡入 + 下移 (200ms)

6. **按钮点击反馈**
   - 轻微缩放：`scale: 1 → 0.95 → 1` (150ms)
   - 霓虹光效：`box-shadow` 闪烁

### 实现方式

```typescript
// 使用 Framer Motion 或 CSS Transition
// 示例：卡片悬停效果
<div className="transition-all duration-200 hover:-translate-y-1 hover:shadow-2xl">
  {/* 卡片内容 */}
</div>
```

---

## 📝 后续开发建议

1. **数据层**: 先使用静态数据（`src/lib/data/`），后续接入 Headless CMS（如 Strapi、Sanity）
2. **图片优化**: 使用 Next.js Image 组件，配置图片 CDN
3. **SEO**: 为每个页面添加 metadata
4. **性能**: 实现图片懒加载、代码分割
5. **响应式**: 移动端优先设计，确保所有页面适配
6. **可访问性**: 添加 ARIA 标签、键盘导航支持

---

## 🚀 快速开始

1. 初始化 Next.js 项目
2. 安装 Tailwind CSS
3. 配置字体（Inter、Space Grotesk）
4. 创建基础布局组件
5. 逐步实现各个页面模块

---

**文档版本**: v1.0  
**最后更新**: 2024







